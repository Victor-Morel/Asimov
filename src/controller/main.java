package controller;
import model.graph.Label;
import model.graph.Node;
import model.graph.StringLabel;

public class main {

    public static void main(String[] args) {

        Controller controller = new Controller(config.path);

        Node n = new Node(new StringLabel("A"), 1, 2);

    }

}
