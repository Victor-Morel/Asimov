package test.model.graph;

import model.graph.Node;
import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Created by Logan Paul on 13/06/2015.
 */
public class NodeTest {

    Node n1;
    Node n2;
    Node n3;

    @Test
    public void testCooling() throws Exception {
      /*  n1 = new Node(5,3,100);
        n2 = new Node(2,2);
        n3 = new Node(6,8,Integer.MAX_VALUE);


        assertNotEquals(90,n1.getIntensity());
        n1.cooling(0.1);
        assertEquals(90,n1.getIntensity());
        n1.cooling(0.5);
        assertEquals(45,n1.getIntensity());

        assertEquals(0, n2.getIntensity());
        n2.cooling(0.5);
        assertEquals(0,n2.getIntensity());

        assertNotEquals(100,n3.getIntensity());
        n3.cooling(0.9);
        assertEquals((int)(Integer.MAX_VALUE * 0.1),n3.getIntensity());
*/
    }

}